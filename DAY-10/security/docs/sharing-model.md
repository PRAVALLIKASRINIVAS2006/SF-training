# Salesforce Security – Record Sharing Model

## Overview

Record-level security determines which specific records a user can access.

The Placement Management System follows a restrictive security approach:

```text
Organization-Wide Defaults
          ↓
     Record Ownership
          ↓
     Role Hierarchy
          ↓
     Sharing Rules
          ↓
     Additional Access
